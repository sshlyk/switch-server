package com.alisa.Iswitch.command;

/**
 * Probably want a mapping from enum name to an actual implementation
 * of some command patter interface.
 * @author parthb
 *
 */
public enum Command {
	ON, OFF;
}
